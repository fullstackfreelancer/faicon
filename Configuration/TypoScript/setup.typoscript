plugin.tx_faicon{
    settings{
        jsonfile = {$plugin.tx_faicon.settings.jsonfile}
        enableBootstrapPackage = {$plugin.tx_faicon.settings.enableBootstrapPackage}
    }
}

page{
    includeCSS{
        tx_faicon = {$plugin.tx_faicon.settings.cssfile}
    }
}
